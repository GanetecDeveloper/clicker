import { css, CSSResult } from "lit-element";

  /**
   * Variables para los sombreados de la aplicación.
   */
  export const themeGlobalShadow: CSSResult = css`
  :host {
    --theme-global-box-shadow: 1px 5px 7px -1px rgba(0, 0, 0, 0.15);
  }
  `;
